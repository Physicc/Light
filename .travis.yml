language: cpp

addons:
  apt:
    packages:
    - cmake
    #OpenGL related dependencies
    - libglfw3-dev
    - libxrandr-dev
    - libxinerama-dev
    - libxcursor-dev
    - libxi-dev
    #Different compilers to test the builds on
    - gcc-8
    - clang-11

env: MESA_GL_OVERRIDE=3.3     

jobs:
  - include:
      - dist: bionic
        compiler:
          - gcc-8
      - dist: bionic
        compiler:
          - clang-11
      - dist: focal
        compiler:
          - gcc-8
      - dist: focal
        compiler:
          - clang-11

script:
  - cmake --no-warn-unused-cli -HEditor -Bbuild -G"Unix Makefiles"
  - cmake --build build
